import react from "React";
import reactDomm from "./React-Dom";
const parent.document.getElementById("root");
const element=React.createElement("h1",{},"Shopping");
  const root=reactDom.createRoot(parent);
  root.render(element);